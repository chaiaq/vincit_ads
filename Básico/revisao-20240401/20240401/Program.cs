// Revisão para prova:
/*
1. Elabore um algoritmo, utilizando pseudocódigo ou linguagem C#, que implementa uma
calculadora com as quatro funções primitivas (adição, subtração, multiplicação e divisão). O
programa também deve implementar um menu com cinco opções, sendo as quatro primeiras
referente às operações aritméticas e, a última, será referente à opção sair. Estruture o programa
baseando-se nos conceitos da programação modular, ou seja, utilize procedimentos e funções.
*/

/*
float valor1, valor2, resultado;
int opcao;

Console.WriteLine("O que deseja fazer?\n1-Somar\n2-Subtrair\n3-Multiplicar\n4-Dividir\n5-Sair");
opcao = int.Parse(Console.ReadLine());

Console.WriteLine("Escolha o primeiro valor: ");
valor1 = float.Parse(Console.ReadLine());
Console.WriteLine("Escolha o segundo valor: ");
valor2 = float.Parse(Console.ReadLine());

if (opcao == 1)
{
    resultado = valor1 + valor2;
    Console.WriteLine("Soma: " + valor1 + "+" + valor2 + " = " + resultado);
}
else if (opcao == 2)
{
    resultado = valor1 - valor2;
    Console.WriteLine("Subtração: " + valor1 + "-" + valor2 + " = " + resultado);
}
else if (opcao == 3)
{
    resultado = valor1 * valor2;
    Console.WriteLine("Multiplicação: " + valor1 + "*" + valor2 + " = " + resultado);
}
else if (opcao == 4)
{
    resultado = valor1 / valor2;
    Console.WriteLine("Divisão: " + valor1 + "/" + valor2 + " = " + resultado);
}
else
{
    Console.WriteLine("Finalizando o programa");
}
*/

/*
2. Utilizando os conceitos aprendidos sobre recursividade, elabore um algoritmo, utilizando
pseudocódigo ou linguagem C#, que converta qualquer número inteiro na base decimal para a base
binária. Utilize o vídeo [Youtube: Como transformar um Número Decimal em Binário],
disponível através do link https://www.youtube.com/watch?v=mttrG_kbHN4, para implementar
o algoritmo recursivo, e identifique o caso base e o caso recursivo antes de escrever o programa.

*/

/*
3. A respeito das subrotinas, faça a associação entre os itens seguir:
E
D
B
C
A
*/

/*
4) Supomos que precisamos elaborar um algoritmo para comer uma pizza grande, com muitas
fatias. Apesar de sabermos que, geralmente, uma pizza padrão tem 8 fatias, não sabemos quantas
fatias a pizza em questão tem. Contudo, os passos para comer a pizza são sempre os mesmos:
1 - pegar uma fatia;
2 - levar a fatia à boca;
3 - comer a fatia;
4 - degustar a fatia;
5 - engolir.
Ao final do processo, retornamos ao primeiro passo, executando o processo até acabar com todas as
fatias da pizza. Analisando a situação, selecione a ÚNICA ALTERNATIVA CORRETA que
expressa o conceito utilizado para elaborar o algoritmo para comer toda a pizza:
a) Recursividade.
*/










